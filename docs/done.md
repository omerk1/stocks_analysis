# Done

Completed work, oldest first. Each entry is a one-line pointer, not the
full story — see `docs/sr_lines_design_notes.md` for the narrative behind
any sr_lines-related entry (root cause, real numbers, what was rejected
before landing on the fix). `docs/backlog.md` tracks what's still open.

Entries are plain bullets tagged with a stable `#N`, not a markdown
ordered list, so the number never silently reflows when an entry is
inserted or reworded (that's why `#14.5` exists: an item that landed
between `#14` and `#15` chronologically). Numbers are permanent
references — other entries and the design notes cite them (e.g. "Done
#22") — so don't renumber on edit, only append new ones at the end.

- **#1** — Project layout + dev environment setup (venv, requirements.txt).
- **#2** — Polygon.io + yfinance data retrieval, SQLite storage.
- **#3** — Reviewed `feature_engineering` indicators, fixed a bug, added missing ones.
- **#4** — Converted `feature_engineering` static-method classes to plain modules.
- **#5** — Storage made source-aware (Polygon/yfinance side by side, source in the primary key).
- **#6** — Resumable bulk ingestion for the whole market, both sources.
- **#7** — Per-ticker reference metadata table (market cap, sector, shares outstanding) from Polygon.
- **#8** — Point-in-time S&P 500/Nasdaq-100 membership tracking from free datasets (cost tradeoff vs. Polygon's paid add-on — see `limitations.md`).
- **#9** — Documented the Polygon/yfinance dividend-adjustment mismatch (different, both-correct conventions).
- **#10** — Fixed a yfinance bulk-ingest resumability bug + added ticker-list scoping.
- **#11** — Reject impossible OHLC values at the storage layer; purged 4,002 already-stored bad rows.
- **#12** — Full active-universe yfinance deep-history backfill (~5,300 tickers, 2010-2026).
- **#13** — **sr_lines milestone-4 checkpoint**: data layer, ATR-adaptive pivot detection, horizontal clustering, event classification, weighted scoring, lifecycle, Plotly chart + CLI.
- **#14** — **sr_lines review round** (AAPL/T/GME/KO): rendering fixes, backtesting/display split, proportional `role_reversal`, ATR%-of-price clustering, dedup rescoring bug. 133 tests passing.
- **#14.5** — `role_reversal` recalibrated to be quality-weighted, not a raw confirmation count.
- **#15** — **Milestone 5: diagonal (RANSAC-style) trendlines** implemented — candidates, event/scoring generalization, `diagonal_penalty`, lifecycle/plotting/CLI wiring. 151 tests passing.
- **#16, #17, #21, #28, #29** — Diagonal candidate generation & dedup: five real-chart-review fixes (pivot-overlap → price-proximity dedup, event/geometry consistency on merge, `first_touch`-on-merge, near-flat/horizontal duplication, candidate-cap fairness).
- **#18, #19** — `duration_density`'s diagonal-bias fix + volume weighting integrated into scoring.
- **#20, #22, #23, #24, #25** — The "hovering" bug and its full resolution arc: `in_play_gate` split out as its own gate, `regime_start` added, a box-rendering overreach corrected, evidence components regime-scoped, event-count saturation windowed.
- **#26, #27** — Chart marker rendering fixes: crossing-bar position, strength-based opacity/size fade.
- **#30** — Weekly-bar detection mechanism, plus two `duration_density` fixes (horizontal/diagonal symmetry, sparse-span density factor) found while reviewing its real output. 181 tests passing.
- **#31** — Historical shares-outstanding ingestion, new `shares_outstanding` table (real time series, unlike `ticker_metadata`'s single overwritten row) backfilled from yfinance's `get_shares_full` — Polygon's equivalent endpoint is `NOT_AUTHORIZED` on our current plan. Raw counts are NOT split-adjusted (confirmed: a ~4x jump lands exactly on AAPL's real 2020-08-31 split date) — a market-cap calc needs to reconcile that against `bars_1d`'s adjusted price convention first, not yet done. Resumable bulk backfill script mirrors `bulk_ticker_metadata_ingest.py`. Schema applied to the real project DB; full-universe backfill not yet run. 193 tests passing.
- **#32** — **`market_common` extraction** (Step 0 of a planned gaps/divergences/fibonacci/avwap effort): pulled `sr_lines`'s timeframe-agnostic data-loading/validation, ATR-ZigZag pivot detection, and shared dataclasses out into a new `src/market_common/` package sr_lines now delegates to (unchanged public signatures, zero call-site changes in `engine.py`/`cli.py`). Indicators (ATR/RSI/MACD/OBV) wrap `feature_engineering`'s existing talib-backed functions rather than reimplementing them. Pivot detection generalized to any single series (RSI, MACD histogram, OBV) or true OHLC high/low, via a pluggable threshold function. New `Pivot.value`/`threshold_at_pivot` fields with read-only `price`/`atr_at_pivot` aliases keep every existing sr_lines call site (candidates.py's clustering/diagonal-fit math) working unchanged. New `data/derived/analysis.sqlite` (separate from the raw price DB) with a shared `runs` table for the modules to follow. Verified: full pre-refactor suite (193 tests) passes unchanged, plus 30 new tests for market_common's own API; real-data cross-check re-confirmed the weekly-resample exact-match property through the new code path. 223 tests passing.

- **#33** — **Four new TA modules built on `market_common`** (gaps, divergences, fibonacci, avwap), each with its own config dataclass, detection logic, SQLite lifecycle tracking in `data/derived/analysis.sqlite`, Plotly chart, and CLI (`python -m src.<module>.cli TICKER|--all --timeframe daily|weekly|both [--as-of] [--plot]`). Built in parallel (four agents, disjoint directories) against a shared, pre-agreed spec. `gaps/`: classic 2-bar + 3-bar FVG detection, ATR-sized, forward-walked fill lifecycle (open/partial/soft_closed/closed). `divergences/`: RSI/MACD-hist/OBV price-divergence detection via paired price/indicator pivots, weighted strength score. `fibonacci/`: multi-scale (2/4/8x ATR) swing detection with cross-scale dedup, retracement/extension levels (linear or log), top-K weighted set ranking, origin-cross invalidation. `avwap/`: pure `anchored_vwap()` computation (zero-lookahead, NaN before anchor) plus anchor discovery (ath/atl/52w high-low/cycle high-low) with same-date dedup, cap-trimming, and staleness tracking. All four share the `runs` table and `ON CONFLICT DO UPDATE` upsert pattern (new to this repo — needed to preserve a row's uuid4 `id` across re-runs without an extra SELECT). 278 tests passing (223 baseline + 55 new). Not yet done: real-universe backfill (all four only run against ad-hoc tickers so far), and everything in the spec's explicit backlog (gap/fib/sr_lines confluence, hidden/triple-pivot divergences, AVWAP stdev bands, a backtest iterator API, etc. — see spec).

- **#34** — Persisted the raw, unclipped inputs behind two existing derived values instead of only their blended/final form. `divergences` now stores `duration_bars`/`price_move_atr`/`indicator_gap_raw` — the three components `strength` caps to [0,1] and weights together before storing (an 8.35x-ATR move and a 50x-ATR move both read `strength=1.0`); `strength` itself is unchanged, still the only field `plotting.py` consumes. `gaps` now stores `bars_to_first_touch`/`bars_to_soft_closed`/`bars_to_closed` alongside their existing date fields, so a bar-count duration doesn't require a re-join against `bars_1d`. Both verified against real data: divergences' new columns hand-matched for a real GME divergence (`price_move_atr=8.35`, `indicator_gap_raw=3.41`); gaps' new columns cross-checked against 542 date↔bar-offset pairs across 200 real NKE gaps, 0 mismatches. 278 tests passing.

- **#35** — **sr_lines: granular touch/break/duration/slope features + persistence.** `Line.n_touches`/`n_wick_fakes`/`n_body_fakes`/`n_breaks` replaced by a structured `TouchCounts` (new `EventType.BODY_TOUCH` splits the old flat "touch" into wick-only vs. body-entering; a resolved `BODY_FAKE` ("Undercut and Rally") now correctly rolls into `TouchCounts.total` alongside the other three held-interaction types). New per-break reclaim tracking (`flip_status.pair_break_reclaims`, distinct from the existing sticky line-level "flipped" state) records whether/how-long-until each individual `BREAK` was later reclaimed. New stored fields: `slope_atr_per_bar` (diagonal, real ATR-normalized slope — also fixed the pre-existing `max_diagonal_slope_atr_per_bar` cap, which had never actually been ATR-normalized despite its name; new default 0.35 verified to pass almost exactly as many real diagonal candidates as the old raw cap did across AAPL/T/GEVO), `age_days_total`/`age_days_regime`/`days_since_last_event`, and `penetration_depth_trend` (closes the open "penetration-depth trend" backlog item, using data already computed per-event). sr_lines also gets real persistence for the first time — new `src/sr_lines/store.py` (`sr_lines` + child `sr_line_events` tables in `data/derived/analysis.sqlite`, mirroring the gaps/divergences/fibonacci/avwap pattern but with a two-table design since a Line carries per-event granularity, and a natural key that includes a rounded `geometry_bucket` since a Line's geometry can legitimately drift run-over-run unlike a Gap's) and a `--all`/persistence-wired `cli.py`. 299 tests passing (26 new).

- **#36** — **fibonacci: per-level touch/respect tracking** (module 1 of the comprehensive-output-model effort across gaps/divergences/fibonacci/avwap). Each `FibLevel` now records what price actually did at that level since it was computed, not just its static ratio→price mapping: `n_touches`/`n_violations` (a violated cross that reclaims within `level_violation_reclaim_bars` still counts as a touch, same "Undercut and Rally" leniency sr_lines gives a reclaimed `BODY_FAKE`), `first_touch_date`/`last_touch_date`, `avg_reaction_atr` (does price actually bounce off this level), and a derived `respected` flag. New `FibConfig` fields: `level_touch_atr_tolerance`, `level_violation_reclaim_bars`, `touch_reaction_window_bars`. New `fibonacci/lifecycle.py` function `evaluate_level_touches`, deliberately run only against a swing's *selected* (top-`max_sets`) levels, not every candidate in `all_sets` — a real per-bar walk per level, wasted work for a swing ranking discards. Closes the "fib level touch/respect scoring" backlog item. Verified against real AAPL/T/GEVO data (idempotent reruns, sane touch/violation/reaction values). 304 tests passing (5 new).

- **#37** — **avwap: interaction/proximity tracking** (module 2 of 4 in the comprehensive-output-model effort). `compute.anchored_vwap()` was already computing a full, zero-lookahead AVWAP series every run and throwing all of it away except `.iloc[-1]`. New `avwap/lifecycle.py`'s `apply_interaction_tracking` walks the same series against `close`/`high`/`low` since the anchor date, populating `AnchoredVwap.distance_atr` (current proximity, ATR-normalized — the direct analog of `sr_lines._proximity`), `n_crosses`/`last_cross_date`, `pct_bars_above`/`pct_bars_below` (a cheap support-vs-resistance signal without a full role/flip state machine), and `avg_reaction_atr_on_touch`. New `AvwapConfig` fields: `distance_tolerance_atr`, `touch_reaction_window_bars`. Verified against real AAPL/T/GEVO data (idempotent reruns; a real 2010 ATL anchor shows `n_crosses=15`/`pct_bars_above≈0.997`, a fresh 2026 ATH anchor shows `n_crosses=0`/`pct_bars_above=0.0` — both exactly the behavior expected). 308 tests passing (4 new).

- **#38** — **divergences: outcome tracking** (module 3 of 4 in the comprehensive-output-model effort, the highest-ML-value gap of the four). Previously a `Divergence` row was a static pattern snapshot — detection stopped dead at `confirmed_at`, with zero record of whether the implied reversal ever happened. New `divergences/lifecycle.py`'s `apply_outcome` walks forward from `confirmed_at` for up to `config.outcome_window_bars` (new field, default 20), populating `max_favorable_move_atr`/`bars_to_max_favorable_move` (ATR-normalized best move in the implied direction) and `invalidated`/`invalidated_at` (did price ever move beyond p2's own extreme against the divergence's thesis). Both tracked independently — an invalidated divergence can still show a real favorable move elsewhere in the window. New `store.py` SET-list inclusion for the outcome fields (unlike `p1`/`p2` geometry, they refresh on rerun as more bars become available). Manually verified one real AAPL divergence's `invalidated_at`/`bars_to_max_favorable_move`/`outcome_computed_through` bar-for-bar against the raw OHLC data — exact match. 315 tests passing (6 new).

- **#39** — **gaps: approach-event counts + post-close reaction** (module 4 of 4, closing out the comprehensive-output-model effort across sr_lines/fibonacci/avwap/divergences/gaps). `lifecycle.py`'s `_walk` already computed a real per-bar intrusion-depth array every run, but collapsed it straight to a single running-max fill curve, discarding the raw signal. New `Gap` fields: `n_approaches` (distinct zone re-entries, not just the one running-max — a gap touched once for 40 bars straight and one touched 10 times briefly previously looked identical), `volume_ratio_at_creation` (rolling-20-bar ratio at the creation bar, same calc as sr_lines' `_vol_ratio`), and `reaction_atr_after_close`/`bars_to_reaction_peak` (once `status` reaches `CLOSED`, the ATR-normalized best move back in the gap's *original* direction within `config.reaction_window_bars` — the classic "fill then reverse" check). Smallest lift of the four modules since `gaps/lifecycle.py` already did exactly this kind of per-bar walk; no new lifecycle module needed, unlike fibonacci/avwap/divergences. Manually verified one real AAPL bearish FVG's `first_touch_date`/`closed_date`/`n_approaches`/`reaction_atr_after_close` bar-for-bar against the raw OHLC data, including a subtle tie-break between two close reaction candidates — exact match throughout. 319 tests passing (4 new).

175 tests passing by the end of the milestone-5 diagonal review arc (#16-#29); exact before/after numbers for every fix above live in `docs/sr_lines_design_notes.md`.

- **#40** — **Macro/meta-financial data ingestion (FRED-first).** New domain for this repo — none of the TA modules or existing ingestion touched macro time series before. Validated live (not just docs) which of the user's candidate free APIs actually work: FRED, BLS's unregistered tier, BEA, and Treasury's `home.treasury.gov` CSV export all responded correctly; Stooq's CSV endpoint is now blocked by a JavaScript proof-of-work bot challenge, not the plain download it's usually described as. Scoped to FRED only — it alone covers ~90% of the requested list (M2, Fed balance sheet, the full Treasury curve, CPI/PCE, GDP, unemployment, credit spreads, a dollar-index substitute, S&P 500 level), and Treasury/BLS/BEA direct integration is deferred to `docs/backlog.md` rather than built speculatively. New `src/data_processing/fred_client.py` (`FredClient`, mirrors `PolygonClient`'s `os.getenv("FRED_API_KEY")` pattern, plain `requests` call, drops FRED's `"."` missing-value sentinel) with a `CURATED_SERIES` starter list of 18 series (incl. `VIXCLS`, the VIX). New `macro_series` table in `market_data.sqlite` (`db.py`) — same shape as `shares_outstanding` with `ticker` swapped for `series_id`, `INSERT OR REPLACE` upsert (refreshes to FRED's latest-known value per date; deliberately not tracking revision vintages). New CLI `src/data_processing/fetch_macro.py` mirrors `fetch_data.py`'s bootstrap. 329 tests passing (8 new: `tests/test_fred_client.py`, plus 4 macro-series round-trip/conflict tests in `tests/test_db.py`).

- **#41** — **Intraday-range-vs-ATR data-quality check** (found via sr_lines but not sr_lines-specific — closes the `docs/backlog.md` item). The existing `validate_bars` soft check only compares close-to-close, so a bar that spikes intraday and fully round-trips by the close was invisible to it; confirmed directly against real data first (T's 2023-01-24 bar: open 15.69, high 17.81, low 15.46, close 15.85 — high is ~13.5% above the open, but close-to-close vs. the prior day's 15.80 close is a completely unremarkable +0.3%). New soft check in `src/market_common/data.py`'s `validate_bars`: flags a bar whose `high - low` exceeds `3.0x` the *prior* bar's `ATR(14)` (prior, not the same bar's own ATR, so the spike can't inflate the very baseline it's measured against; reuses the existing `market_common.indicators.atr` helper, no reimplementation). Surfaced as a new `DataQualityReport.suspicious_intraday_range_dates` field, mirroring `suspicious_jump_dates`'s existing shape exactly — logged, not dropped. Threshold tuned against real data: T's 2023-01-24 ratio is 7.92x (T's own next-highest in ~16 years of history is 5.42x); spot-checked across 10 other real tickers spanning blue-chip (AAPL, MSFT, KO, JNJ, PAAS, MU) to high-volatility/meme names (GEVO, AMC, SMCI, TSLA) — `3.0x` flags roughly 0.1-1.3% of trading days per ticker, confirming it isn't tripping on every normal volatile day while still catching genuine outliers (some of which, like AMC's real 2021 short-squeeze day, are correctly-flagged real events, not data errors). 332 tests passing (3 new, including a real-data acceptance test against the actual stored T 2023-01-24 bar).

- **#42** — **Historical market-cap reconciliation** (the `shares_outstanding`/Done #31 follow-up). New `src/data_processing/market_cap.py`: `reconcile_market_cap(prices, shares, splits)` computes `real_market_cap(d) = bars_1d_price(d) x cumulative_split_ratio(d) x raw_shares(d)` — derived from first principles (a split multiplies real shares and divides real price by the same ratio, so `bars_1d`'s stored price is the real price divided by the cumulative ratio between `d` and now) and verified against real AAPL data before trusting it, not just taken on faith. Splits come from Polygon's `GET /v3/reference/splits` reference endpoint (new `PolygonClient.get_splits`, confirmed live on the free tier — no paid-tier gate, real response cross-checked against AAPL's five actual historical splits) rather than being statistically inferred from `shares_outstanding`'s own jumps, as an initial version of this module did (that inference is now removed from production and repurposed as a `tests/test_market_cap.py` regression fixture proving the explicit-data path reproduces the same validated results on the same scenario).
  Re-verifying end-to-end against real AAPL data surfaced a genuinely important finding, not just a clean confirmation: switching to Polygon's true 2020-08-31 execution date exposed a **separate, pre-existing data-quality gap in `shares_outstanding` itself** that the old statistical-inference version had been accidentally masking. Confirmed directly: AAPL's `shares_outstanding` entry literally dated 2020-08-31 (the real split day) still reports the *pre-split* share count (4,275,630,080) — it doesn't update to the post-split count (17,102,499,840) until an entry dated 2020-10-22, seven weeks later. Since `cumulative_split_ratio(d)` now correctly flips to 1.0 right at the true execution date while a naive `raw_shares(d)` lookup stays stuck at the stale pre-split count until the filing catches up, a first pass at this left `real_market_cap(d)` understated by exactly the split ratio for that whole window (a spurious dip from ~$2.07T on 2020-08-28 to ~$535B on 2020-08-31) — the old inferred-date version avoided this specific symptom only because it derived the split's "date" from that same lagged jump in the first place, so both sides of the formula were laggy together and canceled by coincidence, not because it was more correct.
  Fixed with a new `_bridge_filing_lag`, deliberately narrower than the removed statistical detection: that logic had to *guess whether a split happened at all* from a noisy jump; here the split's existence/date/ratio are already known facts from Polygon, so the only open question is whether a specific `shares_outstanding` row at/after a *known* split's execution date has caught up yet. For each split, it takes the last row before the execution date as the pre-split baseline, then walks forward replacing any row still `scale_consistent` with that baseline (within a new `FILING_LAG_TOLERANCE=1.2`, tighter than the removed detector's 1.5 since this only has to tell "still near a known target" from "diverged," not blind-detect an unknown jump) with `baseline x ratio`, until a row actually diverges (the real filing landing) and is trusted verbatim from there on. Re-verified on real AAPL data: the window is now fully continuous (~$2.07T on 2020-08-28 → ~$2.14T on 2020-08-31 → ~$2.23T on 2020-09-01, synthesized `shares_outstanding_used=17,102,520,320` matching the real eventual filing's 17,102,499,840 to within 0.0001%; no dip anywhere in the full Aug-Nov 2020 window, and the 2020-10-22 handoff from synthesized to real value is seamless). Confirmed correct magnitude elsewhere too (~$1.9T on 2020-08-19, when AAPL genuinely crossed $2T; ~$2.19T on 2020-12-31 vs. AAPL's real ~$2.26T year-end cap).
  15 unit tests in `tests/test_market_cap.py` use synthetic scenarios with hand-computed expected market caps — forward split, reverse split, two compounding splits, sparse as-of alignment, a real-AAPL-style filing-lag window (asserting the fixed, continuous behavior), and a dedicated bridge test proving multiple consecutive stale rows all correct to the same baseline x ratio while organic noise doesn't break "still stale" detection and a real (imperfectly-clean) catch-up filing is trusted verbatim rather than coerced — specifically to catch a multiply-vs-divide direction bug a real-data-only check could miss; `PolygonClient.get_splits` gets 2 more in `tests/test_polygon_client.py`, mocked, no live network in the suite. Read/compute-only against existing tables, no schema change. 347 tests passing (15 new).

- **#43** — **Gap fill-percentage predictive-power analysis.** New standalone, read-only research script `src/analysis/gap_fill_predictive_power.py` (both DB connections opened in SQLite's own `mode=ro` URI mode — a write would fail at the sqlite3 layer, not just by convention). Corrected a stale backlog claim along the way: `fill_by` is NOT a `gaps` column (confirmed by reading `store.py`'s actual `_GAPS_SCHEMA`) — only `GapConfig`/`runs.config_json` carry it, so it's recovered here via a join on `run_id`. Reconstructs each stored gap's full per-bar fill-% time series by replaying `gaps.lifecycle._penetration_pct` (imported, not reimplemented) bar-by-bar from `created_at`, stopping once the running max first hits 100% (mirrors `_walk`'s own `n_approaches`-bounding reasoning — cummax can't move again after that). Correlates four fill-based signals (`peak_fill_pct`, `ever_closed`, `bars_to_threshold`, `bars_to_peak`) against direction-adjusted forward returns at two reference points (N bars after `created_at`, and N bars after the gap's own peak-fill bar) and several horizons (5/10/20/60 bars by default). Verified bar-for-bar against real AAPL data (not just "the script ran"): two real 2010 FVGs hand-computed from raw OHLC match the script's reconstructed series exactly (83.44%/65.22% at first touch, both resolving to 100% on the May 6 2010 flash-crash bar at bar-offsets 30 and 26) — pinned as an actual regression test (skipped, not failed, when local data isn't present). Result against the local universe (AAPL/T/GEVO, 2,124 of 2,126 gaps reconstructed): a real, statistically significant negative correlation between fill depth/completeness and subsequent direction-adjusted forward returns at every horizon tested (Spearman ρ ≈ -0.26 to -0.35, p < 0.001) — deeper/faster/more-complete fills tend to precede weaker continuation or outright reversal versus the gap's own original direction, not the other way around. Flagged honestly, not oversold: `peak_fill_pct`/`bars_to_peak` are computed from the gap's full resolution (however many bars that takes), so the from-creation reference point has a look-ahead overlap at short horizons when a gap closes within the window; the from-peak reference point avoids that specific overlap and shows materially the same negative relationship, which is the more trustworthy of the two. Only 3 tickers in the local universe, so this is a real but narrow-sample finding, not yet validated against the full universe. 350 tests passing (18 new, incl. the real-data regression test above).

- **#44** — **sr_lines per-event touch side + divergences cross-indicator confluence.** Two independent additions answering questions about existing detectors, not new modules. `sr_lines`: `Event.side` ("above"/"below") now records which side price was testing from at each TOUCH/BODY_TOUCH/WICK_FAKE/BODY_FAKE/BREAK — distinct from `Line.origin_side`, which freezes at the line's founding touch and never updates, so it can't answer "which side did a *later* touch, possibly after a flip, come from." `events.py` already tracked this internally via `current_side` (and already correctly re-anchored it to the new side right after a BREAK resolves) — it just wasn't kept on the `Event`. New rollups on `TouchCounts` (`total_from_above/below`, `breaks_from_above/below`, computed once in `lifecycle._derive_event_fields`, the module's one shared home for this kind of aggregate) split the existing `total`/`breaks` by side; `total_from_above + total_from_below == total` always holds. Verified against real AAPL data: a line broken from below later got touched from above (the post-flip side), correctly reflected in both the raw event and the rollup counts. `divergences`: new `Divergence.confluence_count`/`agreeing_indicators` fields report when multiple indicators (RSI/MACD-hist/OBV/volume) diverge off the same underlying price swing — grouped by direction, then clustered by p2-pivot bar-index proximity within `config.pairing_window` (reused, not a new knob — it's already this config's notion of "how close counts as the same pivot," and indicators with differing warmup offsets can land a few bars apart on what's chartwise the same swing). `strength` is deliberately untouched — confluence is reported as an independent signal, not folded into the already-tuned weights. Computed in `detect()` *after* the `as_of` filter, not inside `detect_divergences` — doing it earlier would let a same-pivot divergence from a different indicator that only confirms later leak into a currently-visible row's count, a real look-ahead risk given indicators can each confirm on different lags. Verified against real AAPL data: 30 of 59 divergences cluster with a second indicator. Both DB schema changes follow this repo's existing no-migration convention (widen `CREATE TABLE IF NOT EXISTS` + insert/upsert SQL — `data/derived/analysis.sqlite` is gitignored/disposable). 374 tests passing (9 new).

- **#45** — **FRED macro data: publication dates (ALFRED first-release) + a point-in-time-safe as-of join.** `macro_series.date` (Done #40) is the *period* a value describes, not when it was actually published — e.g. a `GDPC1` row dated for Q2's start isn't really known until ~4 months later. Forward-filling from `date` would leak future information into a backtest. Fixed via FRED's own ALFRED vintage system: `FredClient.get_series_first_release` (`output_type=4`, a wide `realtime_start`/`realtime_end` sentinel range) returns each observation's true first-publication date. Live-verified against the real API for all 18 curated series: works cleanly, full history, for 11 of them (`M2SL`/`WALCL`/`CPIAUCSL`/`CPILFESL`/`PCEPI`/`PCEPILFE`/`GDPC1`/`UNRATE`/`ICSA`/`BAMLH0A0HYM2`/`DTWEXBGS`); 7 can't be served this way (`SP500` isn't tracked in ALFRED at all; `DFF`/`DGS3MO`/`DGS2`/`DGS10`/`T10Y2Y`/`VIXCLS` get a new ALFRED vintage stamped on essentially every business day, exceeding FRED's 2000-vintage-date cap over full history) — `SAME_DAY_PUBLISHED_SERIES` treats these as published same-day instead, which is actually true for them (daily market-quoted values with no real revision lag), not just a workaround. `get_series_first_release` also has a defensive fallback: any FRED-side error response (not just the specific vintage-cap message) is logged and returned as an empty frame rather than raised, so a series creeping past the cap later (`DTWEXBGS` is closest today, 1960/2000) degrades gracefully instead of breaking `fetch_macro.py --series all`. `macro_series` gets two new nullable columns, `published_at`/`first_published_value`, added *alongside* the existing `value`/`date` rather than replacing them — `value` keeps meaning "latest-known/most-revised," exactly as before. New `market_common.macro.as_of_join(bar_dates, macro)` does the actual point-in-time forward-fill via `pd.merge_asof` keyed on `published_at` (not `date`), so a bar can never see a macro value before it was truly knowable. Verified against real data (`fetch_macro.py --series all` rerun against the real `data/raw/market_data.sqlite`, `ALTER TABLE`-widened locally per this repo's no-migration convention): real lags match expectations (`UNRATE` ~1 month, `ICSA` ~5 days, `GDPC1` ~4 months); joining `UNRATE` onto AAPL's real `bars_1d` dates correctly withholds July 2026's reading (4.1, published 2026-08-07) from every bar before that date, showing June's (4.2) instead. `published_at` is null for the oldest stretch of several series' histories (ALFRED's own vintage tracking doesn't reach back to a series' true start — e.g. `M2SL` null 1959-1979, `DTWEXBGS` null through 2019) — expected, not a bug. No consumer wired up yet (see `docs/backlog.md`). 389 tests passing (15 new).

- **#46** — **Market breadth: equal-weight % above SMA(50)/SMA(200)/EMA(8)/EMA(21), golden-cross breadth, advance/decline, for S&P 500 + Nasdaq-100.** New `src/breadth/` module — structurally different from the per-ticker TA modules (sr_lines/gaps/divergences/fibonacci/avwap): breadth is inherently cross-sectional (aggregate *across* an index's constituents on *each date*), not a walk over one ticker's bars, so there's no `lifecycle.py` and no per-event dataclass model, closer in shape to `market_cap.py`'s plain-DataFrame style. Built on `data_processing.db.read_index_membership` (already point-in-time — dated intervals, not a snapshot), which turned out to already be exactly what's needed to avoid survivorship bias with zero new ingestion. `market_common.indicators` gains thin `sma`/`ema` wrappers over the existing `feature_engineering.price_based_indicators` primitives (reused, not reimplemented, same pattern as `atr`/`rsi`/`macd`/`obv`). `compute_breadth` computes each ticker's indicators over its *full* available history before filtering the output window (SMA(200) needs 200 real prior trading days to be valid right at a requested `start` — same pattern `divergences`' `_indicator_pivots` already uses), then interval-joins against membership so a ticker only counts on the dates it was actually a constituent. Deliberately does NOT persist a cumulative advance/decline line — only the daily `net_advances` delta is stored, since a precomputed absolute cumulative value would silently go wrong on any partial/windowed rerun; `advance_decline_line()` computes it on demand from a caller's own full result set instead (same "store raw components, derive at read-time" principle `divergences` already applies to `strength`). New `breadth` table keyed by `(index_name, date)` with no separate `id` — unlike gaps/divergences there's no child table or cross-run identity concern, so it's a plain `INSERT OR REPLACE` (matching `macro_series`), not the id-preserving `ON CONFLICT DO UPDATE` gaps/divergences use. Market-cap-weighted breadth deliberately out of scope (see `docs/backlog.md` for the two concrete blockers). Verified against real S&P 500/Nasdaq-100 data, not just "the script ran": `pct_above_sma50` collapses to 0.9% on 2020-03-12 (the real COVID-crash circuit-breaker day) with `net_advances` swinging ±400+ that same week; 2022's bear-market low lands at 11.2% `pct_above_sma200` on 2022-09-27 (the real 2022 cycle bottom); Nasdaq-100 collapses the same COVID week (2.3% on 2020-03-12) and its `n_constituents` correctly sits at 102-103 (dual-class shares like GOOGL/GOOG legitimately double-count). Also surfaced a real, unrelated data-freshness gap (not a breadth bug): the full-universe yfinance `bars_1d` refresh currently only has solid coverage through 2026-07-28, which `n_with_data` correctly flags rather than silently producing a misleading recent-day percentage (documented in `docs/backlog.md`, not fixed here). Full S&P 500 history run completes in ~25s. 405 tests passing (16 new).

- **#47** — **Local splits cache + scoped shares_outstanding backfill — unblocks market-cap-weighted breadth's data prerequisites.** `market_cap.py`'s `historical_market_cap` made one live, uncached Polygon splits call per ticker per invocation (Polygon free tier: 5 calls/min) — unusable at the ~1,400-ticker (S&P 500 + Nasdaq-100, all-time) scale cap-weighted breadth needs (~4.7 hours of live calls every run). New `splits` table (`data_processing/db.py`, mirrors `shares_outstanding`'s exact shape) + `bulk_splits_ingest.py` (mirrors `bulk_shares_outstanding_ingest.py`'s resumable-via-`fetch_jobs` pattern — one call per ticker, since `PolygonClient.get_splits` already returns a ticker's *entire* split history in one call). `historical_market_cap` gets a new optional `splits` param — when given (e.g. `db.read_splits(conn, ticker)`), skips the live call entirely; omitted, unchanged original behavior, so no existing caller breaks. New shared `db.read_index_universe_tickers(conn, index_names)` helper (`SELECT DISTINCT ticker FROM index_membership WHERE index_name IN (...)`, all-time members not just current) feeds both the new splits script and a new `--indices` option on `bulk_shares_outstanding_ingest.py`, scoping that backfill to exactly S&P 500 + Nasdaq-100 (~1,400 tickers) instead of the full ~5,304-ticker active universe (still available, unchanged, as the default when `--indices` is omitted). Verified against real data, not just mocked tests: backfilled real splits for AAPL (5 real splits incl. the known 2020-08-31 4-for-1) and TSLA (2020-08-31 5-for-1, 2022-08-25 3-for-1) and real `shares_outstanding` for AAPL (340 points), then called `historical_market_cap(..., splits=db.read_splits(conn, "AAPL"))` with a mock Polygon client asserted never called — reproduced the exact market-cap figures already hand-verified in Done #42 (~$2.07T on 2020-08-28 → ~$2.14T on 2020-08-31 → ~$2.23T on 2020-09-01) entirely from the local cache. The full ~1,400-ticker backfill runs (shares_outstanding: well under an hour, yfinance; splits: ~4.7 hours, Polygon's 5/min cap) are run separately as a live data operation, not gated on this PR (`data/raw/market_data.sqlite` is gitignored). **Still open, deliberately**: wiring an actual cap-weighted mode into `src/breadth/compute.py` itself — this PR only unblocks the data.

Running the real shares_outstanding backfill surfaced a second, genuine bug (not flakiness): `YFinanceClient.get_shares_outstanding` unconditionally called `.tz_convert("UTC")` on the returned index, which requires a tz-*aware* index -- yfinance actually returns a tz-*naive* index for some tickers (confirmed live: several delisted/acquired names -- X, XEC, XLNX, YHOO), raising `TypeError: Cannot convert tz-naive timestamps`. All 167 of the first real backfill run's failures (out of 1,255 tickers attempted) were this exact error, not unrelated flakiness -- fixed by only calling `tz_convert` when `index.tz is not None`, skipping straight to `.normalize()` otherwise. Verified live post-fix: ticker `X` (previously failing) now returns 475 real shares-outstanding points. 420 tests passing (15 new).

- **#48** — **Chart pattern detection, Phase 0/1: design doc + Double Top/Bottom vertical slice.** New `src/patterns/` module (design doc: `docs/features/chart_pattern_detection_design.md`; running narrative + phase-by-phase progress tracker: `docs/features/chart_pattern_detection_design_notes.md` — check that file's "Progress / build order" section for what's landed vs. still ahead before resuming this work). Phase 0: shared toolkit (`models`/`config`/`base`/`trendlines`/`volume`/`scoring.py`) built to reuse `market_common.pivots.detect_pivots` (already the design doc's ATR-scaled ZigZag spec) rather than reimplementing it; extracted the one genuinely shared trendline primitive (least-squares fit) into `market_common/trendline_fit.py`, wired into `sr_lines/candidates.py` too. Phase 1: first full vertical slice (detector → lifecycle → scanner → store → cli → plotting) via a Double Top/Bottom detector — deliberately chosen over Head & Shoulders as the first pattern since its 3-pivot shape reuses the same neckline/measured-move logic H&S needs with less to build first. New generic `lifecycle.py` state machine (`PENDING`/`CONFIRMED`/`ACTIVE`/`HIT_TARGET`/`INVALIDATED`/`INVALIDATED_FAILED_BREAKOUT`/`EXPIRED`) built pattern-agnostic from the start (a `trigger_at(bar_index)` callable, not a flat level) so later, sloped-boundary patterns can reuse it unchanged. `pattern_matches` table follows sr_lines' own precedent of one table + a discriminator column (`pattern_type`) rather than a table per pattern type; current-state-only like every other derived table here (a later run overwrites an earlier run's confidence/status on the same natural key) — a dedicated as-of backtest harness is deliberately deferred (see `docs/backlog.md`). New `backtest/labeler.py` is a human-in-the-loop CLI for building the design doc's §7.1 ground-truth labeled set, started in Phase 1 rather than deferred so the label set grows opportunistically as later phases land. One real bug found+fixed while writing Phase 1's synthetic tests: `has_prior_trend`'s first implementation rejected/accepted based on distance from the search window's found extreme to the pivot, which broke on a flat-plateau-then-spike fixture (the extreme sat at the flat plateau's start, trivially far from the pivot, even though the actual move was a 2-bar spike) — traced to the design doc not actually requiring anti-spike filtering at §3.1 (that's specific to cup & handle's own §4.4 roundedness check), fixed by simplifying to a plain magnitude-over-available-history measurement. Verified end-to-end against real AAPL history (105 patterns, sane status distribution across every terminal state, geometry spot-checked directly in the derived DB — `target_price`/`stop_price`/`entry_price` correctly ordered on the right side of the neckline for both directions, zero out-of-range confidence values). 522 tests passing (20 new). PR #39.

- **#49** — **Chart pattern detection, Phase 2: Head & Shoulders + Inverse.** New `src/patterns/detectors/head_shoulders.py`, extending Phase 1's double top/bottom vertical slice from 3 pivots to 5 (see `docs/features/chart_pattern_detection_design_notes.md`'s own Phase 2 section for the full narrative). Reuses `lifecycle.py`'s state machine completely unchanged — its `trigger_at(bar_index)` callable was already generic enough to accept a sloped neckline (fit via `trendlines.fit_line` through the two trough/peak pivots) with zero changes there, confirming the Phase 1 design held up. Two decisions worth flagging: (1) `target_price` is computed off the neckline's own value at the right shoulder's bar index rather than the design doc's literal "neckline at breakout," since `apply_lifecycle` needs one fixed target float before it discovers where the breakout even happens — a constraint double top/bottom's flat neckline never surfaces, since there "neckline at breakout" and "neckline at formation end" are the same number by construction; (2) new hard gates beyond double top/bottom's single symmetry check — `head_exceeds_shoulders` (strict; also covers the doc's separate "right shoulder above head → reject" case, since if RS > head then head > RS already fails), `head_shoulders_min_leg_bars=5` (the doc's literal "each leg ≥ 5 trading days"), and a new `head_shoulders_neckline_max_slope_pct=10.0` cap (the doc names no figure — "cap slope... reject if... >X%" — so this is a first-pass starting point, same unvalidated status as every other knob here). New `scoring.hs_price_symmetry`/`hs_time_symmetry` implement §6.1's explicit H&S formulas (normalized by head height, not `avg(a,b)` like double top's own `price_symmetry`), blended 50/50 into `geometric_cleanliness` in place of double top/bottom's price-only version. `plotting.py` generalized to draw a sloped neckline from `PatternMatch.trendlines["neckline"]` when a detector supplies one (§5's data model already had this field; double top/bottom never populated it), falling back to the old flat `key_levels["neckline"]` line otherwise — sets up triangle/wedge boundaries in Phase 3 for free, no further plotting changes anticipated there. Verified end-to-end against real AAPL history: 118 total patterns (105 double top/bottom unchanged from Phase 1 + 13 new: 8 head_and_shoulders, 5 inverse_head_and_shoulders), spot-checked geometry directly in the derived DB (head strictly exceeds both shoulders in every row, `target_price`/`stop_price` correctly ordered on the right side of the neckline for both directions, zero out-of-range confidence values). 533 tests passing (11 new). PR #41.

- **#50** — **Chart pattern detection, Phase 3: Triangles (asc/desc/symmetric) + Wedges.** New `src/patterns/detectors/triangles.py` — one detector for all five shapes, per the design doc's own module layout (see `docs/features/chart_pattern_detection_design_notes.md`'s own Phase 3 section for the full narrative). The one genuinely new architectural problem this phase introduces: a triangle's breakout side isn't fixed by the pattern's own geometry the way H&S/double-top's is — a close beyond *either* boundary confirms, direction only known once it happens (§4.3: a "wrong-side" break must be tagged, not discarded). Resolved with two additions: `Direction.NEUTRAL` on the shared `market_common.models.Direction` enum (checked first that `gaps`/`divergences` never construct anything but `BULLISH`/`BEARISH`, so this is purely additive for both), and a new `lifecycle.apply_lifecycle_bidirectional` entry point alongside the existing `apply_lifecycle` — refactored the post-breakout half of the original function into a shared `_walk_post_breakout` first so both entry points call the exact same target-hit/failed-breakout/active logic, not two copies; reran Phase 1/2's full test suites immediately after the refactor, before writing any triangle code, to confirm zero behavior change. Also added an optional `pending_deadline_bar_index` override (§4.3: a triangle also expires at its own apex, on top of the standard formation-duration deadline) — computed as `min(standard_deadline, apex_bar)` entirely in the detector, no new expiry logic in `lifecycle.py` itself. New `trendlines.convergence_apex_bar`/`r_squared` for §6.1's trendline-fit/apex geometry. One real bug caught during design, before it shipped: a first pass at §6.1's "range narrows leg-over-leg" cleanliness metric evaluated the *fitted* boundary lines' own range at each pivot — since range between two linear boundaries is itself perfectly linear, and the convergence hard gate already requires `upper_slope < lower_slope`, this would have scored 1.0 for every triangle that passed the hard gate, a non-differentiating metric masquerading as signal. Fixed by computing it off the raw pivot-to-pivot price legs instead (same idea as VCP's own §4.5 contraction-leg definition), which actually varies per candidate. A second real bug caught by code review before merge: the apex-based `pending_deadline` floored the fractional apex bar (`int(apex_bar)`), which silently discarded a genuine breakout whenever the true apex landed inside the very first bar past the window's end (the walk's first loop iteration would already be past the floored deadline); fixed with `math.ceil(round(apex_bar, 6))` — ceiling to grant that bar a real chance, rounded first so `np.polyfit`'s own floating-point noise (a mathematically-exact apex coming back as e.g. `44.00000000000008`) doesn't spuriously round up an extra whole bar. Both the original bug and the fix's own edge case are now permanent regression tests. `plotting.py` generalized from a single hardcoded "neckline" lookup to drawing every entry in `match.trendlines` (one for H&S, two — upper/lower — for a triangle/wedge). Verified end-to-end against real AAPL history (daily + weekly): 226 total daily patterns (118 unchanged from Phase 1/2 + 108 new: 56 rising_wedge, 17 ascending_triangle, 14 descending_triangle, 14 falling_wedge, 7 symmetric_triangle) — confirmed bidirectional resolution actually produces *both* outcomes for a biased shape, not just the expected one, directly in the derived DB (e.g. `ascending_triangle` shows real `bullish` *and* `bearish` rows), zero `direction='neutral'` rows persisted (every symmetric triangle in AAPL's history had already resolved by the run's cutoff), `target_price`/`stop_price` correctly ordered on the right side of `direction` for all 108 resolved rows, zero out-of-range confidence values. 565 tests passing (32 new, including a new `tests/test_patterns_lifecycle.py` for the bidirectional entry point directly). PR #42.

- **#51** — **Chart pattern detection, Phase 4: Cup & Handle + Inverse.** New `src/patterns/detectors/cup_and_handle.py` + `src/patterns/curves.py` (`fit_roundedness`, isolated and independently tested first, per the module's own progress-tracker note) — see `docs/features/chart_pattern_detection_design_notes.md`'s own Phase 4 section for the full narrative. Scoped to Cup & Handle + Inverse only, not Rounding — the progress tracker's Phase 4 title said "+ Rounding" but its own Phase 6 entry independently deferred Rounding until after phases 1-5 are validated; resolved in favor of the more detailed Phase 6 wording, a pre-existing inconsistency in the plan rather than a new scope cut. The one genuinely new structural problem this phase introduces: unlike every prior pattern's fixed-size pivot window, §4.4's own pivot sequence allows "possibly several pivots" forming the rounding — an unknown count, not a fixed 3/5/6. Resolved by decoupling pivots from geometry: the pivot scan only anchors three fixed points (left rim, right rim, handle — scanned as bounded `(rim1_index, rim2_index)` pairs, handle always the very next pivot after rim2 by strict alternation), while roundedness is checked against the *raw close-price path* between the rims (`curves.fit_roundedness`, not the pivots). Unlike H&S/triangle's sloped or two-sided boundaries, cup & handle's flat trigger level (`right_rim.price`) never moves, so `target_price` needed no "value at formation end, not literal breakout" approximation — the design doc's literal §3.6 formula applies exactly. One real finding, checked deliberately rather than assumed: a plain symmetric V-shape (same width/depth as a realistic cup) scores R²≈0.94 against a parabola — comfortably above the default `cup_roundedness_min_r2=0.5` floor, meaning R² alone does *not* reliably distinguish a real cup from a sharp V (matches the design doc's own framing of R² as one of "two practical approaches," not a claimed-perfect discriminator, not a bug); recorded so a future reviewer doesn't assume otherwise, and flagged as worth revisiting if real chart review shows V-shaped false positives are common. `min_r2=0.5` still does real work against genuinely degenerate cases (a single-bar spike scores R²≈0.03). A real bug caught by code review before merge: `plotting.py`'s `key_levels["neckline"]` fallback drew the trigger line starting from `match.pivots[1]`, correct for double top/bottom's fixed 3-pivot list (index 1 is the trough) but wrong for cup & handle's variable-length list, where index 1 is just whatever pivot follows the left rim, not the right rim that actually set the trigger price; fixed to `pivots[-2]` (the real invariant both patterns share — the trigger pivot is always second-to-last, "index 1" only coincided for double top/bottom because its list happens to be exactly 3 long), with a permanent regression test confirmed to fail against the pre-fix code. Verified end-to-end against real AAPL history: 299 total patterns (226 unchanged from Phase 1-3 + 73 new: 72 cup_and_handle, 1 inverse_cup_and_handle — the 72-vs-1 skew tracks AAPL's own predominantly-uptrending history, since inverse cup & handle requires a qualifying ≥30% downtrend into the left rim, materially rarer than the ≥30% uptrend the regular pattern needs), spot-checked geometry directly in the derived DB (0 rows with target/stop on the wrong side of direction, cup_extreme below both rims and handle in the upper half held on every sampled row, zero out-of-range confidence values). 583 tests passing (18 new). PR #43.

- **#52** — **Chart pattern detection, Phase 5: VCP.** New `src/patterns/detectors/vcp.py` — see `docs/features/chart_pattern_detection_design_notes.md`'s own Phase 5 section for the full narrative. The most architecturally novel detector so far, in two ways anticipated since Phase 0/1 but not built until now: (1) it's the first to run its own, *finer* `detect_pivots` pass (`vcp_pivot_atr_mult=1.0` vs. the scanner's shared coarse 2.5) rather than consuming the `pivots` argument `scan()` receives — `base.py`'s own docstring updated to document this exception, since it previously claimed flatly that "detectors never call detect_pivots themselves"; (2) it gates on a Trend Template regime check (price above rising 150/200-day MAs, 50-day MA stacked above both, near its own 52-week high) *before* even examining a candidate base's contraction geometry — treated as the doc's own more specific stand-in for "prior uptrend" (§4.5 point 1 explicitly pairs the two), not a separate generic check on top. A real calibration finding, checked deliberately rather than assumed: an initial `vcp_atr_contraction_max_ratio=0.6` (already looser than the doc's own "commonly ~1/3" textbook figure) produced *zero* real matches against AAPL's full daily history — traced before accepting it, not shrugged off as "this pattern is just rare": 393 real candidates cleared the monotonic-depth and higher-lows gates, but every one of their `ATR(short)/ATR(long)` ratios sat between 0.68 and 1.81 (median ~1.03), meaning a shrinking *percentage* retracement (what the pivot-based depth/higher-lows checks measure) doesn't reliably predict a shrinking *ATR* (what the ratio measures) — a leg can retrace a smaller % of price while still being individually choppy. Raised the ceiling to 1.0 and re-verified: 167 real matches on AAPL daily, 24 on weekly (confirming the weekly `PRESETS` overrides — 10/30/40-week MAs, ATR(2)/ATR(10) — actually work, not just in unit tests); `scoring.contraction_tightness_score` still rewards a tighter ratio continuously within the new ceiling, so only the hard gate moved, not what counts as clean. Test fixtures lean on a genuinely useful property discovered while building them: on perfectly linear price data, `SMA(N)` at bar `i` is just the price `N/2` bars back, so a plain rising ramp satisfies every Trend Template condition (MA stack, both MAs rising, near the 52-week high) automatically — no hand-tuned shape needed, unlike cup & handle's roundedness fixture. Verified end-to-end against real AAPL history (daily + weekly): 466 total daily patterns (299 unchanged from Phase 1-4 + 167 new VCP) spread across every terminal status; spot-checked geometry directly in the derived DB (0 rows with `target_price <= stop_price`, several sampled rows correctly show exactly one non-shrinking contraction pair — confirmed this is the doc's own explicit one-violation tolerance working as intended, not a bug, before treating it as expected), 0 out-of-range confidence values. 595 tests passing (12 new). PR #44.

- **#53** — **Chart pattern detection, Phase 6 (bonus): Flags/Pennants + Rounding.** See `docs/features/chart_pattern_detection_design_notes.md`'s own Phase 6 section for the full narrative. Rounding landed inside `detectors/cup_and_handle.py` itself, per the design doc's own §8 module layout ("cup and handle without the handle") — refactored the existing detector so every shared gate (rim symmetry, depth, prior trend, roundedness) is checked once, then the presence of a valid handle right after rim2 decides Cup & Handle vs. Rounding (no valid handle, including no handle pivot at all) as the fallback classification, rather than running two independent scans that would produce near-duplicate matches for the same base. A genuine refactor of already-merged Phase 4 code, not a bolt-on — caught and updated two of Phase 4's own existing tests whose "no match at all" assertion for an invalid handle was no longer correct (now "falls through to Rounding instead"), confirmed each rewritten test would fail against the old code first. New `src/patterns/detectors/flags_pennants.py` reuses triangle's own convergence math for the pennant variant (a pennant is "a small symmetric triangle," per §4.6's own precedent). Two real calibration findings, both checked before shipping rather than assumed, mirroring VCP's own Phase 5 discipline: initially built against the scanner's shared coarse pivot pass (on the strength of §4.7's "cheap given zigzag infra" framing), which produced zero real matches — traced it to the coarse pass's 11-bar median pivot gap making a 4-pivot consolidation window span 35-85 bars, nowhere near "much shorter than the patterns above"; became the second detector after VCP to run its own finer `detect_pivots` pass (`flag_pivot_atr_mult=1.5`, checked to bring the median gap to 4 bars) rather than loosen the duration ceiling and erase what makes a flag a flag. Still zero real matches after that fix — a second calibration issue, `flag_consolidation_max_range_ratio`'s initial "textbook" 0.5 ceiling, real candidates clustered between 0.64 and 1.15; raised to 1.0 and re-verified. Building synthetic test fixtures for a detector with its own internal pivot pass (unlike every hand-built-pivot-list detector's tests) surfaced two real `detect_pivots` mechanics worth recording for any future such detector: a pivot's confirmation threshold uses ATR at that pivot's *own* bar, so a sharp pole's peak needs a proportionally large reversal to confirm (a flat lead-in never confirms a starting pivot at all, since it never reverses); and a consolidation pivot only confirms once a *later* bar reverses far enough away from it, so a "shared prefix, different tails" test design needs the confirming reversal baked into the fixed prefix, not left to a per-test tail. A second real bug caught by code review before merge, in `plotting.py`: the Phase 4 `pivots[-2]` fix (docs/done.md #51) assumed "second-to-last" was the universal invariant for the neckline pivot, true for double top/bottom and cup & handle (both followed by one more pivot) but false for Rounding, whose window ends *at* the right rim with nothing after it (index -1, not -2) — the same class of bug the -2 fix itself replaced, reintroduced by the first pattern type without a trailing pivot. Fixed by locating the neckline pivot by price instead of position (searched from the end of the list, since an earlier pivot — confirmed for real in the existing shared test fixture — can share the exact same price as the true neckline pivot), with a new regression test confirmed to fail against the pre-fix code first. Verified end-to-end against real AAPL history (daily + weekly): 516 total daily patterns (466 unchanged from Phase 1-5 + 50 new: 46 rounding_bottom, 1 rounding_top, 1 bull_flag, 1 bear_flag, 1 pennant), 131 total weekly patterns (up from 107); spot-checked geometry directly in the derived DB (0 rows with target/stop on the wrong side of direction across both new pattern families, 0 out-of-range confidence values). 610 tests passing (25 new). PR #45.
