# Core Data Science
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.14.0
scikit-learn>=1.5.0

# Technical indicators (requires the TA-Lib C library: `brew install ta-lib` on macOS)
ta-lib>=0.5.1

# Data retrieval
requests>=2.32.0
yfinance>=0.2.0
curl_cffi==0.7.4  # lets yfinance impersonate real browser TLS fingerprints, reducing block risk;
                  # newer curl_cffi (>=0.15, yfinance's own recommendation) failed to import on
                  # this dev machine with a native-extension linking error (dlopen: symbol not
                  # found '_CFRelease') -- 0.7.4 is the newest version confirmed working here
polygon-api-client>=1.14.0
beautifulsoup4>=4.12.0

# Modeling / optimization
catboost>=1.2.7
pygad>=3.5.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.24.0

# Config / utilities
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.29.0

# Dev / testing
pytest>=7.3.0
black>=24.10.0
flake8>=6.0.0
isort>=5.12.0
